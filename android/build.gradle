apply plugin: 'java'

repositories {
	google()
	jcenter()
	mavenCentral()
}

dependencies {
  compile 'com.ramotion.paperonboarding:paper-onboarding:1.1.3'
}

configurations.all {
  transitive = false
}

task getDeps(type: Copy) {
	from sourceSets.main.runtimeClasspath
	into 'lib/'
}